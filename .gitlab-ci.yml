stages:
  - build
  - test
  - deploy

# --- Build Stage ---
build-job:
  stage: build
  image: python:3.10
  before_script:
    - pip install --upgrade pip
    - pip install -r requirements.txt
  script:
    - echo "Build stage dependencies installed"

# --- Test Stage ---
unit-test-job:
  stage: test
  image: python:3.10
  services:
    - name: postgres:17
      alias: test_db
  variables:
    POSTGRES_DB: test_heroes
    POSTGRES_USER: test_hero
    POSTGRES_PASSWORD: test_hero
    POSTGRES_HOST_AUTH_METHOD: trust
  before_script:
    - pip install --upgrade pip
    - pip install -r requirements.txt
  script:
    - echo "Running unit tests..."
    - pytest -v --maxfail=1 --disable-warnings

lint-test-job:
  stage: test
  image: python:3.10
  before_script:
    - pip install --upgrade pip
    - pip install flake8
  script:
    - flake8 app tests --select=E9,F63,F7,F82

# --- Deploy Stage ---
deploy-job:
  stage: deploy
  script:
    - echo "Deploy stage would deploy application here..."

